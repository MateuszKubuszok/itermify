title: Alacrity Itermified
shell:
  program: tmux
  args:
    - -L
    - itermified
    - -f
    #- ~/.itermify/tmux.conf # alacritty passes it raw so TMUX cannot find this file :(
    - /Users/dev/.itermify/tmux.conf # workaround for Alacritty not expanding ~/
import:
  - ~/.config/.alacritty.yml
